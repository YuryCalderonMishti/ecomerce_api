<div class="max-w-2xl mx-auto mt-10 bg-white shadow-lg rounded-2xl p-6">
    <h1 class="text-3xl font-bold text-pink-600 mb-4 text-center">{{product.title}}</h1>

    <div class="space-y-3">
        <p class="text-gray-700"><span class="font-semibold">Descripción:</span> {{product.description}}</p>
        <p class="text-gray-700"><span class="font-semibold">Precio:</span> ${{product.price}}</p>
        <p class="text-gray-700"><span class="font-semibold">Categoría:</span> {{product.category}}</p>
        <p class="text-gray-700"><span class="font-semibold">Stock disponible:</span> {{product.stock}}</p>
    </div>

    <form action="/api/carts/685b6ab1a5a62a2cb1b60cfd/product/{{product._id}}" method="POST" class="mt-6">
        <button
        type="submit"
        class="w-full bg-green-500 hover:bg-green-600 text-white py-2 px-4 rounded-lg text-lg font-semibold transition duration-200"
        >
        Agregar al carrito 🛒
        </button>
    </form>

    <div class="text-center mt-4">
        <a href="/products" class="text-blue-600 hover:underline text-sm">← Volver a productos</a>
    </div>
</div>
